# 🖼️ Public Assets (`/public`)

This directory holds static assets that do not need to be processed by Webpack/Vite, such as the `favicon.ico` and the application screenshot images used in the main README.