# 🧠 TrustScope Backend

This directory contains the Python FastAPI backend for TrustScope. It handles the machine learning pipelines (Random Forest, Gradient Boosting, Isolation Forest), NLP sentiment extraction (VADER), and serves the application.